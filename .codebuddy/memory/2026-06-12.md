# 2026-06-12 工作日志

## 中观热力图三项修复

1. **热力图居中** — `MesoLayout.jsx` HeatmapChart 容器添加 `margin: '0 auto'`
2. **行业分类调整** — "环保""社会服务"(申万2021版)/"休闲服务"(申万2014版) 从默认周期兜底移入 **强防御** DEFENSIVE_NAMES。同频更新测试文件 `MesoHeatmap.test.js` 的 SW_FIRST_LEVEL 列表和 DEFENSIVE_NAMES 集合
3. **下钻日期维度** — `IndustryDrilldown` 组件 `subIndustries` useMemo 中新增 `target.date` 分支：用 `l2Parsed.matrix[si.name]?.[target.date]` 覆盖 `si.change`，使下钻 treemap 展示的是点击日期而非最新日的涨跌幅；同时新增 `industry_daily_query` 查询 + `DataChart` 渲染一级行业历史走势图

## 中观热力图 UI 精简（第二轮）

1. **删除非图表文字** — 移除 HeatmapChart 容器内的分类描述行和底部提示行，只保留纯图表
2. **高度裕余** — `h = max(320, N*36+60)` → `max(380, N*40+90)`，为 visualMap + 日期轴留更多底部空间
3. **控件行水平排列** — "💡 点击方格下钻二级行业"提示文字移至控制栏右侧（`marginLeft: auto`），与分类按钮/天数/均涨同行；"显示天数"缩短为"天数"，input 宽度 52→46

## 中观热力图 融通宏观周期（第三轮）

1. **visualMap 右下角** — `orient: 'horizontal' + left: 'center' + bottom: 0` → `orient: 'vertical' + right: 4 + bottom: 40`；grid 右侧留80给色温条
2. **强周期按三周期分组排序** — 新增 `CYCLE_GROUPS` 映射（kitchin: 钢铁/采掘/汽车/非银金融/综合, juglar: 机械设备/建筑材料/建筑装饰, kuznets: 房地产），`sortByCycleGroup()` 函数控制热力图 Y 轴行业排列顺序
3. **三周期数据面板 `CyclePhasePanel`** — 强周期模式**左侧**面板，纯 `CardWrapper` + 正常文字展示（不用 DataCard），字体微软雅黑，`minWidth: 260, maxWidth: 340` 自适应宽度
4. 并排布局：面板在左 `flex: '0 0 auto'` + 热力图在右 `flex: 1`，面板不遮挡 Y 轴行业名

## 全局字体统一微软雅黑（第四轮续）

- `global.css` body `font-family: var(--font-sans), serif` → `var(--font-sans)`（去掉 serif 回退，`--font-sans` 已含 `'Microsoft YaHei'` 首选）
- `CyclePhasePanel` 移除冗余 `fontFamily: yahei`，继承全局
