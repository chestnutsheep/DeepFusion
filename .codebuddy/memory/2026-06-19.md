# 2026-06-19

## IDE 配置清理：删除冗余 .iml 模块文件

清理了 `.idea/` 目录中由 JetBrains IDE 跨平台迁移（Windows `G:\PycharmProjects\DeepFusion` → Linux `/home/AI/workspace/Mcp Server/DeepFusion`）和重复加载产生的冗余壳模块：

- 删除 `deep-fusion@1.iml`（空壳，仅引用 deep-fusion）
- 删除 `deep-fusion@2.iml`（空壳，仅引用 DeepFusion）
- 从 `.idea/modules.xml` 中移除上述两个模块的注册行

保留：
- `DeepFusion.iml`（主模块，手动配置的 sourceFolder + SDK `uv (DeepFusion)`）
- `deep-fusion.iml`（pyproject.toml 自动生成的 PYTHON_MODULE）

注意：`.gitignore` 第 163 行 `#.idea/` 被注释掉，导致 `.idea/` 被 git 跟踪。如需进一步消除 git status 噪音，可去掉该注释并执行 `git rm -r --cached .idea/`。

## 反诈个股深度分析功能迁入（从 `改动/` 目录）

将 `改动/DeepFusion/` + `改动/fusion-web/` 的反诈分析功能迁入主项目，采用方案 A（重构式合并，不保留 `core/` 目录）。

### 后端
- 新建子包 `deep_fusion/shared/anti_fraud/`，含 4 个文件：
  - `__init__.py`：导出 StockCode/resolve_stock_code/5 个数据簇函数/build_report
  - `akshare_api.py`：从 `改动/DeepFusion/core/akshare_api.py` 原样迁移（含 StockCode 智能解析器 + 暴雷分析六大模块数据获取）
  - `data_clusters.py`：从 `改动/DeepFusion/core/data_clusters.py` 迁移，导入路径 `from core.akshare_api` → `from .akshare_api`
  - `report_builder.py`：从 `改动/DeepFusion/core/report_builder.py` 原样迁移（纯计算，无依赖）
- 新建 MCP 工具 `deep_fusion/tools/anti_fraud.py`：注册 `anti_fraud_report` 工具，去掉原版 `sys.path.insert` hack，改用 `from ..shared.anti_fraud import ...`
- `deep_fusion/__init__.py`：tools 导入列表和 `__all__` 新增 `anti_fraud`（字母序，analysis 之后 bonds 之前）
- **关键修复**：akshare 返回的 DataFrame 含 `date`/`Timestamp`/`NaN`/numpy 类型，`json.dumps` 无法序列化。在 `anti_fraud.py` 加 `_json_default` 处理器，处理 datetime/date/Timestamp/np.integer/np.floating/np.ndarray/float NaN

### 前端
- 新建 `dashboard/src/components/Micro/AntiFraudPanel.jsx`：iframe 注入 `anti-fraud-template.html` + `INJECTED_REPORT`
- 新建 `dashboard/public/anti-fraud-template.html`：ECharts 渲染 7 块报告
- 修改 `dashboard/src/components/Micro/StockPanel.jsx`：新增 import AntiFraudPanel + showAntiFraud state + 🛡️反诈分析按钮 + 显示分支
- `dashboard/vite.config.js`：新增 `host: '0.0.0.0'`（局域网访问），保留现有 timeout 和 vitest 配置

### 验证
- lint 全部通过
- 子包导入 + StockCode 解析正常（`002598` → sz002598/SZ002598/深市）
- MCP 工具注册成功（126 个工具，含 anti_fraud_report）
- 端到端调用 `anti_fraud_report(symbol='002598', concept='钠电池')` 成功，返回 7 块 REPORT JSON

### 未迁移
- `改动/fusion-web/.../MesoLayout.jsx` 的 `import AntiFraudPanel` 行未合并（误加，MesoLayout 是行业面板，与反诈无关）
- `改动/DeepFusion/core/` 目录未保留（内容已全部迁入 `deep_fusion/shared/anti_fraud/`）

### 注意
- `data_clusters.py` 中的 `_individual_info`/`_financial_indicators` 等函数与现有 MCP 工具（stocks.individual_info 等）功能重复，但返回格式不同（dict vs CSV/text）。当前保留独立实现，未做去重，未加 ak_cache 缓存。未来如需去重需重构现有工具暴露底层 API。
